{
  "name": "whatchardoing3-backend",
  "version": "1.0.0",
  "description": "Backend server for What Char Doing polling app",
  "main": "server.cjs",
  "type": "commonjs",
  "scripts": {
    "start": "node server.cjs",
    "health": "echo 'Backend is healthy'",
    "dev": "node server.cjs",
    "build": "echo 'Backend requires no build step'",
    "frontend:dev": "cp package.frontend.json package.json && npm install && npm run dev",
    "frontend:build": "cp package.frontend.json package.json && npm install && npm run build"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "socket.io": "^4.7.4"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
